---
// src/pages/blog/index.astro
import { posts } from '../../data/posts';
---

<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <title>Aerospace 블로그</title>
  </head>
  <body class="bg-slate-50 min-h-screen p-10">
    <div class="max-w-4xl mx-auto">
      <h1 class="text-4xl font-bold mb-10 text-center">📝 개발 블로그</h1>
      
      <div class="grid gap-6">
        <!-- 데이터를 순회하며 카드 만들기 -->
        {posts.map((post) => (
          <a href={`/blog/${post.slug}`} class="block group">
            <article class="bg-white p-6 rounded-xl shadow-sm border border-slate-200 hover:shadow-md hover:border-blue-300 transition-all">
              <h2 class="text-2xl font-bold text-slate-800 group-hover:text-blue-600 transition-colors">
                {post.title}
              </h2>
              <p class="text-slate-500 mt-2 line-clamp-2">{post.content}</p>
              <div class="mt-4 text-sm text-slate-400 font-mono">{post.date}</div>
            </article>
          </a>
        ))}
      </div>

      <div class="mt-10 text-center">
        <a href="/" class="text-slate-500 hover:text-slate-800">🏠 홈으로 가기</a>
      </div>
    </div>
  </body>
</html>